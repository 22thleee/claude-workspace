/*
 * portfolio/config.js 의 템플릿입니다.
 *
 * 직접 쓰지 말고, repo 루트의 .env.local 을 채운 뒤
 *   node scripts/gen-config.mjs
 * 를 실행하면 portfolio/config.js 가 자동 생성됩니다.
 *
 * (수동으로 만들려면 이 파일을 config.js 로 복사하고 값을 채우세요.)
 */
window.PORTFOLIO_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
